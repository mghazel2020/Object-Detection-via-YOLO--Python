# Object-Detection-via-YOLO--Python
In this project, we shall demonstrate the Deep Learning (DL) inference using a DL object detection model, YOLO-v3, which has already been trained on the COCO dataset. 
